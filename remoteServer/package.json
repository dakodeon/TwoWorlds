{
  "name": "remote_server",
  "version": "1.0.0",
  "description": "Remote server for the two worlds project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Loui the Bugger",
  "license": "ISC",
  "dependencies": {
    "dns": "^0.2.2",
    "express": "^4.13.3",
    "externalip": "^1.0.1",
    "public-ip": "^1.1.0"
  }
}
